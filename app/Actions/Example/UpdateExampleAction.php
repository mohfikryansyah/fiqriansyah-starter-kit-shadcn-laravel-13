<?php

namespace App\Actions\Example;

class UpdateExampleAction
{
    /**
     * 
     * 
     */

    // public function handle(Post $post, array $data): Post
    // {
    //     return DB::transaction(function () use ($post, $data) {
    //         $post->update([
    //             'title' => $data['title'],
    //             'content' => $data['content'],
    //             'status' => $data['status'],
    //         ]);

    //         return $post->refresh();
    //     });
    // }
}
